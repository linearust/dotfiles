# .claude

Shared Claude Code configuration. Use as submodule.

## Add to project

```bash
git submodule add https://github.com/linearust/.claude.git .claude
```

## Update

```bash
cd .claude && git pull
```

## Override

Add project-specific `CLAUDE.md` in project root to extend these rules.
